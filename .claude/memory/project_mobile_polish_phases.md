---
name: project-mobile-polish-phases
description: "Mobile redesign on feat/mobile-polish-phase1 — Phase 1 (foundation), Phase 2 (orange rebrand, unified dock, de-chroming) and a dead-code cleanup all pushed 2026-08-31; Players/Splits/Moments interiors pending"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8ef91dc7-61ba-46ea-a621-aad8ac4a1baf
  modified: 2026-09-01T06:16:52.311Z
---

The cricket app is getting a phased mobile-first polish (target 375–430px; desktop only needs to stay functional; the existing PWA is deliberately untouched).

**Phase 1 — DONE (2026-08-31, branch `feat/mobile-polish-phase1`, user opens the PR themselves):** `viewportFit: 'cover'` enabled (safe-area env() now real in browser Safari; header got safe-area top padding), global tap-highlight removal, `.pressable` + `.animate-view-in` + `.pb-cricket-nav` utilities and `--cricket-content-bottom` token in globals.css, toast `mobileOffset` above the nav pill, Drawer/ComposerModal safe-area bottom padding, Input 15→16px (stops iOS focus-zoom), 44px hamburger/dialog-close/segmented tabs, new shared `ActionSheet` (bottom-sheet ⋮ menu taking the same `CardMenuItem[]` as CardMenu), CardMenu vertical flip fix, ShareFab on the shared Drawer, EditExpenseDrawer→ComposerModal, and the balance-first Expenses recomposition (greeting+season pill one line; pool hero first; carried-forward entry moved INSIDE ExpenseList via a new `carriedSlot` prop; Expenses header moved above the list; metric strip is soft tiles not a hairline grid).

**Phase 2 — DONE (2026-08-31, pushed; three commits total on the branch):** full visual-system redesign. Theme tokens overhauled in BOTH themes (light = cool neutral `#EEF0F3` canvas + white cards, dark = neutral charcoal ladder, replacing mint + navy); brand switched from blue to Sunrisers orange with a new **`--cricket-on`** token (text on orange fills: white in light, dark ink in dark — never `text-white` on a cricket fill); the **runtime DB color override was REMOVED** from Shell.tsx per user decision — `cricket_teams.primary_color` is inert, tokens are the single source of truth (docs/MULTI_TEAM_DESIGN.md documents why); **`CRICKET_GLOBAL_NAV`** (components/cricket-global-nav.ts) is the one dock vocabulary on every cricket page, with contextual Upcoming/Completed/Stats as a SegmentedControl at the top of Matches + league-stats (dock stays on 'matches' there); SegmentedControl rebuilt with a translateX sliding elevated-chip indicator; CricketSectionNav's underline replaced by a measured sliding tint pill; primary cricket Button is SOLID orange (gradients deliberately dropped); sheet/dialog backdrops lost their full-screen blur (perf); pool hero = border-free elevated card with a full-bleed signed 2×2 ledger (hairline separators, `+`/`−` signs); borders stripped from month list, stat tiles, match cards, sponsor cards, FeeTracker pool card, carried row, season pill (elevated chip), filter chips. Dead code deleted: CapsuleTabs (entirely unused!), modern-mobile-menu + its ~120-line CSS block, PendingMembers. All 38 remaining keyframes verified in use (script audit).

**Phase 3 — DONE (2026-08-31, pushed).** Umpiring art-direction: editorial type-led header (no icon chip), neutral elevated status module + RosterHero (semantic color only in icons/donut), match-duty cards lost border + colored left edge (open-slot rows keep their dashed orange — the call to action), status pills → icon+text, quiet tonal filter rail (only the active filter takes its semantic tint), guests as a hairline section, "I'll do it"/"Give up" 32→40px. Players: Add/Edit form → ComposerModal (manual iOS body-scroll-lock deleted), all three hand-rolled w-[340/360px] portals → shared Dialog max-w-xs/sm, both ⋮ → ActionSheet, roster = ONE continuous elevated surface with hairline dividers (captain/VC/admin border-color coding replaced by chips + "· Admin" metadata text), inputs 14→16px, selected chips → tonal (the old --cricket-accent fill + white text failed dark-mode contrast), photo hover-overlay → always-visible camera badge moved below inputs, expanded-details clamp 300→480px (was silently clipping). **PlayerProfile deliberately stays a Dialog** — its edit mode embeds text inputs and vaul is banned for those; converting means extracting the edit form to ComposerModal first.

**Phase 3.1–3.3 QA pass — DONE (2026-08-31, pushed).** SummaryStats values neutral (only a negative pool balance reads red; icons carry the stat colors; the current-view tile's tint is the emphasis mechanism on both Players and Fees). Player rows: role pill → quiet colored text in one metadata line; signed-up dot's animate-ping removed. Add Player form grouped Identity/Contact/Team role with hairline section headers; guest-toggle knob on the spring token; Button disabled = opacity-45 + saturate-50 (app-wide). PlayerProfile: 22px name, de-bordered attribute/contact rows, edit-mode selectors tonal (fixed dark-mode white-on-orange). FeeTracker Team Pool: 30px dominant collected figure, solid-cricket h-1.5 bar (gradient dropped), tonal Edit. MatchSchedule ⋮ → shared ActionSheet (CardMenu removed there). LeaderboardCard: mouseenter chevron tint removed (dead on touch). League-stats table deliberately untouched — it already implements both the frozen-column scroll table AND a persisted card-view toggle; don't churn its documented width tuning. GOTCHA hit twice now: a {/* JSX comment */} placed as a SIBLING inside a parenthesized `return (`/`{cond && ( ... )}` expression is a parse error — comments only between JSX children.

**Phase 3.2B + Venkat identity fix — DONE (2026-09-01, pushed).** Fees payment list is a LEDGER: one elevated surface per section (Recently paid / Still to pay) with `divide-y` hairlines, rows flat (viewer's row = 6% brand tint), Mark paid borderless tonal, ⋮ → ActionSheet (anchor-map machinery deleted), `animate-view-in` on rows so a marked player visibly arrives in the paid section. **Financial-safety pattern (keep):** the roster has TWO Venkats and truncated rows nearly caused wrong-player payments — fee rows now show the FULL name (wraps, never truncates), `#jersey · $amount · status` meta line, Mark paid confirms via a Dialog naming name+jersey+amount, and the button's aria-label carries the identity. `FeeRow` is exported purely for `tests/unit/fee-row-identity.test.tsx` (4 tests pin all of this). Also: CodeQL alert #43 on PR #47 ("DOM text reinterpreted as HTML" at the player photo preview) assessed as FALSE POSITIVE — `URL.createObjectURL` blob: URL into an img src, no HTML sink; user dismisses it in the Security tab (work-account gh lacks code-scanning scope on the personal repo).

**Phase 3.4/3.5 + Splits/Sponsors/Stats batch — DONE (2026-09-01, pushed).** Sponsors: continuous ledger surface, quiet one-line audit trail, green amount TEXT not Badge, KPI SummaryStats removed from the sponsors view only. Splits deep rework: tabs → shared SegmentedControl with `Label · count`; hero de-striped; Balances headers lead with calculated totals; proportion-bars removed; all 4 hand-rolled portals + the filter popover → shared Dialog/ActionSheet; every gradient + text-white on cricket fills → solid + `--cricket-on`; **Prev/1/2/Next pagination → LoadMore (cumulative slice, "Showing X of Y")**; settlements show FULL names. **Activity row went through 5 iterations — final frozen form:** `[avatar] title(≤2 lines) + right total / payer(11.5px semibold muted, playerLabels shortName, NEVER "Paid by" visibly, NEVER truncated) `· date`(10.5px dim nowrap unit) / YOU-OWE caps-label + 12.5px bold amount`; participant count/receipts/Added-by live ONLY in the expansion (whose first line is `date · N people · Added by X`); full "Paid by <fullname>" sentence lives in the row aria-label. SplitForm: player grid 4→3 cols with playerLabels disambiguation + aria-pressed, category chips tonal, searches 16px, All↔Clear toggle. Stats 3.3B: StatChip pills → quiet text metadata (all four tab heroes), detail-sheet timeline de-framed. **Workflow gotcha: NEVER `rm -rf .next`/`next build` while the user's dev server runs — it kills it with Internal Server Error. Checkpoint with `tsc --noEmit` + vitest; build only immediately before commit and warn the user.** Bottom clearance on /cricket verified: pb-cricket-nav + h-24 spacer + footer mb-24 — don't add more padding when someone reports mid-scroll FAB overlap.

**Fees final IA + roster scoping batch — DONE (2026-09-01, pushed to main directly; user said feature branches no longer needed, PR #47 merged).** Fees went through SEVEN sequential briefs landing as one final state: (1) SummaryStats tiles removed from fees view (players-only now); (2) page order = compact `YourFeeStatus` line (48px tinted row: ✓ paid green / (!) due orange, remainder as figure for partial — replaced the big bottom YourFeeCard, date/marked-by live only on the player's own Paid row) → Still to pay card → unpaid list → Paid card → Team Pool → reminder; (3) BOTH state sections are matching elevated summary cards — `(!) STILL TO PAY · 11 PLAYERS / 11 fees due · $60.00 each / $660.00` (orange accent, list on separate surface below) vs `(✓) PAID · 8 PLAYERS / 8 fees collected · $60.00 each / $480.00` (green ✓, rows inside card; renamed from "Recently paid" — it holds ALL paid players). "· $X each" self-hides when subtotal ≠ count×fee (partials). Both sections **default collapsed** (user call) and expand INDEPENDENTLY — the shared-accordion state made opening one collapse the other and read as jump-scrolling; (4) FeeRow: stable right amount column (remaining owed for partial), meta = `#n · Owes team` / `✓ Fee paid · date · logged by X` ("logged by", never "by" — direction ambiguity), names line-clamp-2, **Mark paid = solid `var(--cricket)` + `--cricket-on` + glow** after a grey pass read as disabled — semantic system: orange=do this, green=done, grey=secondary; (5) ShareFab hidden on fees view only (`activeView !== 'fees'` in page.tsx — the reminder button IS the fees share); (6) Team Pool spacing tightened one notch. Tests: fee-row-identity grew direction cases (Owes team present, "Paid by" absent, logged-by pinned) — 750 total. Same batch: **PlayerManager Squad list is now season-scoped** (shared `seasonRoster`, off-roster actives → collapsible "Not in <Season>" + admin "Add back", section absent in fallback mode; CLAUDE.md roster bullet updated); Team Header (greeting wraps clamp-2, bat icon dropped, quieter season chip w/ remount transition + "Season: X" aria); PlayerProfile edit/close fixes (Edit right-14 clear of X, quiet orange text action, `showClose={!editing}` kills the double-X in edit mode); shared Dialog close now 44px (p-3.5, right/top-1.5) with per-instance `closeLabel` prop.

**Season-scoped captaincy — CODE PUSHED to main 2026-09-01; SQL AWAITING the user running `docs/season-designation-migration.sql` in Supabase.** User bug: designation lived on `cricket_players`, so promoting a Fall captain rewrote Spring's history everywhere. Model = the is_guest two-facts playbook: `cricket_season_players.designation` (season truth, partial unique indexes per season scoped `left_at IS NULL`) + `cricket_players.designation` kept as CURRENT mirror (fallback mode + cross-team RPC payloads), synced from an ACTIVE season by the `set_season_designation` RPC (SECURITY INVOKER, reason codes ok/bad_designation/no_season/not_on_roster/conflict, validates+locks the target BEFORE displacing — the council's shared HIGH was that a late not_on_roster return COMMITS the incumbent's demotion). Full 3-agent council review ran (DBA/RLS/SQL); other fixes: mirror clear by team_id not roster membership (two-captains drift), backfill gated to first-run-only, unique_violation→'conflict', COALESCE(is_active,false), search_path='', mirror ROW_COUNT raises. DB trigger `trg_clear_designation_on_deactivate` frees the unique slot on deactivate/move-to-guest. Client: `seasonRoster().designationOf()` is the ONLY read path (PlayerManager chips/form/conflict-check, greeting, ShareButton ×3, PlayerProfile, DutyPlayerSheet); store `setSeasonDesignation` (falls back to record-level writes when the season has no roster rows — RPC would refuse); PlayerManager routes designation AFTER updatePlayer/addPlayer, NEVER in payloads (a designation:null in the update payload wipes the mirror), and confirmDesignationSwap no longer strips the incumbent before the form is even saved. 757 tests. **Until the user runs the migration, assigning a captain in cloud mode fails with a toast (RPC missing) — reads are unaffected (select('*') + fallback).**

**Still pending:** Moments comment layer (20–22px targets, 13–14px inputs zoom on iOS), the deep Splits rework (still has gradients + text-white on cricket fills + hand-rolled SettleUpModal/portals), scoring screens (guarded by docs/SCORING_UI.md), PlayerProfile→sheet conversion (needs edit-form extraction). Sticky finances tab bar is blocked by `overflow-hidden` on the page container (`app/(tools)/cricket/page.tsx`).

**Verified findings worth remembering:** Tailwind v4 already gates `hover:` utilities behind `@media (hover: hover)` — no global hover variant change needed. `gh` CLI is logged in as the work account `bmantralaupgrade`, which cannot create PRs on the personal `BhaskarMantralaHub/vibe-planner` repo — the user creates PRs there themselves. Full plan: `/Users/bmantrala/.claude/plans/eventual-hugging-popcorn.md`.

Related: [[project_cricket_separation]]
